// import PrintifyClient from '../src';

describe('blah', () => {
  it('works', () => {
    expect(true).toBeTruthy();
    //  const client = new PrintifyClient({
    //   apiKey: process.env.PRINTIFY_API_KEY!,
    //   shopId: process.env.PRINTIFY_SHOP_ID,
    // });
  });
});
